import 'package:flutter/cupertino.dart';

class ContentListSubTitle extends StatelessWidget {
  String subTitle;
  ContentListSubTitle({Key? key, required this.subTitle}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
